# Instagram Insights Collector MVP Requirements

- 作成日: 2026-09-07
- 担当: 佐伯 真琴（System Integration PM）
- 依頼元: 水野 葵
- 対象媒体: Instagram
- 目的: 投稿動画のインサイトを投稿後30日間、時系列データとして自動収集・保存する
- status: requirements_defined

## 1. 目的

Instagram APIを利用して、自分のInstagramアカウントへ投稿した動画のインサイトを定期取得し、将来AIが分析できる時系列データを蓄積する。

MVPは収集と保存までを責務とし、分析、評価、改善提案は行わない。

## 2. MVP Scope

- 対象Instagramアカウントの投稿動画を自動的に収集対象へ追加する。
- 各投稿について、投稿時刻から30日間、1時間ごとを目標にインサイトを取得する。
- 取得したインサイトをDynamoDBへ時系列で保存する。
- 投稿後30日を超えた投稿は新しいインサイトの取得対象から除外する。
- 30日経過後も、保存済みデータは削除しない。
- AWSリソースはCloudFormationで再現可能にする。
- GitHub Actionsから検証・デプロイ・環境設定を実行できるようにする。
- セットアップ、運用、認証更新、障害確認、復旧、QA結果をナレッジとして残せる状態にする。

## 3. Functional Requirements

### FR-01 投稿検出

- 対象アカウントに新しく公開された動画投稿を検出する。
- 同じ投稿を重複して収集対象へ登録しない。
- MVPで対象とするInstagramのメディア種別は、API仕様確認後に明記する。

### FR-02 定期取得

- 1時間単位の定期実行で、投稿後30日以内の対象投稿を抽出する。
- 対象投稿ごとにInstagram APIから、その時点で利用可能なインサイトを取得する。
- 一時的なAPI失敗時は、安全な範囲で再試行する。
- 取得できない指標や投稿があっても、処理全体の結果を追跡できるようにする。

### FR-03 保存

- 同一投稿・同一観測時刻枠について冪等に保存する。
- インサイト値に加え、データの識別と検証に必要な最小限のメタデータを保存する。
- 分析結果、スコア、投稿評価、改善案は保存しない。

### FR-04 取得終了

- 投稿日時から30日を超えた投稿は、次回以降のAPI取得対象に含めない。
- 取得終了後の保存済みインサイトは、将来の分析利用のため保持する。

### FR-05 デプロイと運用

- CloudFormationで必要なAWSリソースを作成・更新・削除できる。
- GitHub Actionsから環境別にデプロイできる。
- 認証情報をGit管理対象へ平文保存しない。
- 処理失敗、認証失効、取得漏れの兆候を運用者が確認できる。

## 4. Data Requirements

最低限、次の情報を保存する。実際の指標名と型はInstagram APIの公式仕様・実機検証後に確定する。

- Instagramアカウント識別子
- メディア識別子
- メディア種別
- 投稿日時
- 観測日時
- 投稿からの経過時間
- 取得できたインサイト指標と値
- 使用したAPIバージョン
- 取得状態またはエラー分類

将来のMCPから安全に参照できるよう、投稿と観測値を一意に特定できる構造を必須とする。MCP用APIや書き込み機能そのものはMVP外とする。

## 5. Non-functional Requirements

- 最小構成を優先し、MVPに不要なコンポーネントを追加しない。
- AWS無料利用枠内での運用を第一目標とする。
- 無料利用枠を超える可能性がある条件を、投稿数・API取得数・Lambda実行時間・DynamoDB・ログ量ごとに明示する。
- 二重実行や途中失敗があっても、重複データや対象漏れを抑えられること。
- トークンなどの秘密情報をログへ出力しないこと。
- ログの保持期間を定め、無制限に費用が増えないこと。
- 障害時に再実行または復旧できること。

## 6. Provisional Technology Constraints

依頼元の希望技術は次のとおり。構成と責任境界はarchitecture担当の検証後に確定する。

- EventBridge Schedulerまたは同等のAWSスケジュール機能
- AWS Lambda
- Amazon DynamoDB
- AWS CloudFormation
- GitHub Actions

1時間ごとの取得要件があるため、「毎日1回のLambda起動」ではなく、1時間単位で処理を開始できる方式を前提候補とする。

## 7. Out of Scope

- インサイトの分析、投稿評価、改善提案
- AIによる分析
- MCPサーバーとMCPツール
- MCPからDynamoDBへの書き込み
- Vercelを含むダッシュボードやグラフ表示
- 30日経過後の継続取得
- 複数Instagramアカウント対応（初期MVPでは1アカウントを前提）

## 8. Acceptance Criteria

- [ ] 新しい動画投稿が重複なく収集対象になる。
- [ ] 投稿後30日以内は、1時間ごとの観測値がDynamoDBへ保存される。
- [ ] 一時的な失敗や再実行があっても、同一投稿・同一観測枠のデータが重複しない。
- [ ] 投稿後30日を超えると、新しいAPI取得が行われない。
- [ ] 保存済みインサイトは30日経過後も参照できる。
- [ ] CloudFormationとGitHub Actionsにより、手順どおり再現可能にデプロイできる。
- [ ] 秘密情報がGitおよび通常ログへ平文で残らない。
- [ ] 想定利用量に基づく月額試算と、無料枠を外れる条件が記録されている。
- [ ] APIエラー、認証失効、取得漏れを検知・確認する運用手順がある。
- [ ] System QAが取得周期、保存内容、冪等性、30日停止、障害復旧、デプロイ再現性を確認し、passedを記録する。
- [ ] セットアップ、運用、認証更新、障害対応、復旧、QA結果のナレッジ引継ぎが作成される。

## 9. Decisions Pending Architecture Validation

- Instagram APIで利用する認証方式、必要権限、トークン更新方法
- 対象にできる動画種別と取得可能なインサイト指標
- 投稿検出方法と検出遅延
- 1時間枠の定義、許容遅延、欠損時の扱い
- DynamoDBのキー、インデックス、保存単位
- リトライ、レート制限、部分失敗、バックフィル方針
- シークレットの保管先とGitHub Actionsからの安全な設定方法
- 監視・通知・ログ保持の最小構成
- 想定投稿数を置いた無料枠試算

## 10. Later Phases

1. 保存データを閲覧する最小ダッシュボードまたはVercel UI
2. 読み取り専用MCPによるAI分析
3. 必要性と安全性を確認した場合のみ、MCP書き込み機能

